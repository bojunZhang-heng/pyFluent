[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zbj_utils"         # 包名（与包文件夹一致），不要用横线
version = "0.0.1"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]  # 告诉 setuptools 在 src 目录里寻找包

